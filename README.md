# SVM-Iris-Classification

This project demonstrates the implementation of a Support Vector Machine (SVM) classifier using the Iris flower dataset in Python. The notebook includes data visualization, train-test splitting, model training, evaluation using confusion matrix and classification report, and hyperparameter tuning with GridSearchCV using Scikit-Learn.
